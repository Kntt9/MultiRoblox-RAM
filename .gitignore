# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.claude
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Deploy output (gerado por site/build-deploy.mjs)
deploy

# Artefatos de download do site — commite-os (gerados por build.bat / tauri build)
!dist/
!dist/MultiRoblox.exe
!dist/*-setup.exe

# Cópia local do app 0.5.0 usada só para testar o auto-update — não commitar
# (gerada manualmente; apague quando não precisar mais)
dist/MultiRoblox_0.5.0_portable.exe

# Chaves de assinatura do auto-update — NUNCA commitar
src-tauri/.updater-secret/

# Pasta de build do Rust (gigante, gerada)
src-tauri/target/

# Release local gerada por scripts/make-release.mjs
release/
